[project]
name = "annotix-ml"
version = "0.1.0"
description = "Annotix auto-labeling sidecar: baseline YOLO inference over HTTP"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "ultralytics>=8.3",
    "requests>=2.31",
]

[tool.uv]
package = false
